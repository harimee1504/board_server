{
    "version": 2,
    "functions": {
        "api/**/*.py": {
            "includeFiles": "api/graphql/**/*.graphql"
        }
    },
    "rewrites": [
      { "source": "/(.*)", "destination": "/index" }
    ]
}